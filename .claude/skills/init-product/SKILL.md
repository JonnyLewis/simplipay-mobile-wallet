---
name: init-product
description: Bootstrap a brand-new product from an idea into this codebot kit. Claude Code wrapper for the shared init-product workflow.
---

# init-product

This Claude Code skill is a thin wrapper.

Read and follow the shared workflow body:

```text
workflows/agent/init-product.md
```

Rules for this wrapper:

- Do not duplicate or reinterpret the workflow here.
- Use Claude Code tools and interaction style to execute the shared workflow.
- Keep `AGENTS.md` as the shared operating manual.
- Keep `CLAUDE.md` as the thin Claude Code entrypoint importing `@AGENTS.md`.
